version: '3'
services:
    api-bexs:
        build: .
        container_name: bexs_desafio_dev
        volumes : 
            - .:/app
        command: bash -c "python3 manage.py runserver 0.0.0.0:8000"
        ports:
            - "8000:8000"
